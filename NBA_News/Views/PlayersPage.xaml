<Page x:Class="NBA_News.Views.PlayersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#0D1117">

    <Page.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#111827"/>
            <Setter Property="AlternatingRowBackground" Value="#1A2340"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#1F2937"/>
            <Setter Property="VerticalGridLinesBrush" Value="#1F2937"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D1117"/>
            <Setter Property="Foreground" Value="#A0AEC0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1E3A5F"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A2340"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Joueurs NBA" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <TextBlock x:Name="SubTitle" Text="Chargez les joueurs pour les afficher"
                       FontSize="14" Foreground="#A0AEC0" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#111827" BorderBrush="#1F2937"
                    BorderThickness="1" CornerRadius="8" Margin="0,0,10,0">
                <Grid>
                    <TextBox x:Name="SearchBox" Background="Transparent" Foreground="#E0E0E0"
                             FontSize="14" Padding="12,10" BorderThickness="0" CaretBrush="White"
                             TextChanged="SearchBox_TextChanged"/>
                    <TextBlock x:Name="SearchPlaceholder" Text="🔍  Rechercher un joueur par nom..."
                               Foreground="#4B5563" FontSize="14" VerticalAlignment="Center"
                               Margin="12,0" IsHitTestVisible="False"/>
                </Grid>
            </Border>
            <Button Grid.Column="1" x:Name="BtnSearch" Content="Rechercher"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0" Click="BtnSearch_Click"/>
            <Button Grid.Column="2" x:Name="BtnLoad" Content="⟳ Charger"
                    Style="{StaticResource ActionButton}" Background="#1F2937" Click="BtnLoad_Click"/>
        </Grid>

        <Border Grid.Row="2" CornerRadius="10" Background="#111827" ClipToBounds="True">
            <DataGrid x:Name="PlayersGrid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"          Binding="{Binding Id}"          Width="60"/>
                    <DataGridTextColumn Header="Nom complet" Binding="{Binding FullName}"     Width="180"/>
                    <DataGridTextColumn Header="Poste"       Binding="{Binding Position}"     Width="70"/>
                    <DataGridTextColumn Header="N° Maillot"  Binding="{Binding JerseyNumber}" Width="90"/>
                    <DataGridTextColumn Header="Pays"        Binding="{Binding Country}"      Width="120"/>
                    <DataGridTextColumn Header="Draft"       Binding="{Binding DraftYear}"    Width="70"/>
                    <DataGridTextColumn Header="Équipe"      Binding="{Binding TeamName}"     Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusBar" Text="Prêt" Foreground="#4B5563" FontSize="12"/>
                <StackPanel x:Name="LoadingPanel" Orientation="Horizontal" Visibility="Collapsed" Margin="12,0,0,0">
                    <ProgressBar IsIndeterminate="True" Width="80" Height="6"
                                 Foreground="#3B82F6" Background="#1F2937" Margin="0,0,8,0"/>
                    <TextBlock Text="Chargement..." Foreground="#3B82F6" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border x:Name="ErrorPanel" Grid.Row="2" Background="#1C1010" BorderBrush="#E94560"
                BorderThickness="1" CornerRadius="8" Padding="20" Visibility="Collapsed" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="❌ Erreur" Foreground="#E94560" FontSize="16" FontWeight="Bold" Margin="0,0,0,6"/>
                <TextBlock x:Name="ErrorMessage" Foreground="#CC8888" FontSize="13" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>